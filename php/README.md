## php 이미지 설명

| 태그명                  | 기반 OS                  | Debian 릴리스 이름                 | 출시 시기          | 특징                                               |
| ----------------------- | ------------------------ | ---------------------------------- | ------------------ | -------------------------------------------------- |
| **8.2-apache**          | Debian 기반 (기본값)     | 보통 최신 안정(stable)을 자동 선택 | 흐름에 따라 변경됨 | 이미지를 사용할 때 최신 Debian stable로 따라감     |
| **8.2-apache-trixie**   | Debian 13 (testing)      | Trixie                             | 2025~ (정식 예정)  | 최신 패키지, 불안정 가능성, 개발/테스트 용         |
| **8.2-apache-bookworm** | Debian 12 (stable)       | Bookworm                           | 2023~              | 현재 가장 안정성 높은 Debian stable. 실서비스 권장 |
| **8.2-apache-bullseye** | Debian 11 (oldstable)    | Bullseye                           | 2021~              | 오래됐지만 안정적. 일부 패키지가 구버전            |
| **8.2-apache-buster**   | Debian 10 (oldoldstable) | Buster                             | 2019~              | 공식 지원 거의 종료. 보안 업데이트 제한            |

## 🔨 1. Docker 이미지 빌드

프로젝트 루트에서:

```sh
docker build -t my-php-app:latest .
```

my-php-app이라는 이미지가 생긴다.

## ▶️ 2. Docker 컨테이너 실행

```sh
docker run -d --name myweb -p 8080:80 -v $(pwd)/src:/var/www/html my-php-app:latest
```

### 옵션 설명

- -d → 백그라운드 실행
- -p 8080:80 → 호스트 8080포트 → 컨테이너 80포트
- --name → 컨테이너 이름 지정
- my-php-app → 아까 빌드한 이미지 이름

## 🌐 3. 결과 확인

브라우저에서:

http://localhost:8080

## 4. docker compose

```sh
docker compose up -d
```
